# Changelog - 2026-02-16

### 14:00 - Implementar tabs multi-servidor, auto-puerto y colores por server

- **Nuevo**: `src/view/ServerTab.java` (~250 líneas) - Encapsula todo el estado y UI de un servidor individual (proceso, consola, writer, puerto, DiscoveryResponder, color de tab)
- **Nuevo**: `src/view/ServerPortManager.java` (~40 líneas) - Gestión de puertos para evitar conflictos entre servidores simultáneos
- **Modificado**: `src/view/ThemeManager.java` - Añadido `isDarkMode()` multiplataforma (Windows + macOS), refactorizado `setupSystemTheme()` para usarlo
- **Modificado**: `src/minecraftServerManagement/ForgeUtils.java` - Refactorizado `getServerOutputs()` con callback `Runnable onServerReady` en lugar de referencias hardcodeadas a MainFrame
- **Modificado**: `src/view/MainFrame.java` - Eliminados ~15 campos estáticos de servidor, reemplazado contentPane con JTabbedPane + List<ServerTab>, nuevo sistema de tabs con colores y botón cerrar (×), auto-apertura del último servidor, ventana ampliada a 900x550, paleta de 6 colores con variantes light/dark
- **Modificado**: `src/view/GeneralConfigurationsWindows.java` - Actualizado para usar ServerTab activo en lugar de MainFrame estáticos
- **Modificado**: `src/view/GoogleWindows.java` - Clone de servidor abre nuevo tab en lugar de reemplazar contentPane
- **Modificado**: `src/jgit/GitUtils.java` - Reemplazadas referencias a MainFrame.serverProcess/serverWriter/serverIsOn/serverOpenedDirectory con métodos estáticos delegados
- **Modificado**: `src/minecraftServerManagement/CustomCommands.java` - Igual que GitUtils, usa métodos estáticos delegados
- **Modificado**: `src/cloud/google/GoogleDriveCloudProvider.java` - Reemplazadas referencias a MainFrame estáticos con métodos delegados

**Paquetes afectados**: view, minecraftServerManagement, jgit, cloud.google
